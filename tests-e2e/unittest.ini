[WebApplication]
Uri = http://localhost:9036

[pytest]
testpaths = 
    play
; addopts = --headed --slowmo 2000
log_cli = true  
log_level = DEBUG  
log_format = %(asctime)s %(levelname)s %(message)s  
log_date_format = %Y-%m-%d %H:%M:%S
log_file = logs/pytest-logs.txt
